import { StyleSheet } from 'react-native';
import { Color } from '../../../../utils/StylingConsts/Colors/Colors';

export const stylesDiabeties2MeasuresPage = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Color.colorWhite
  }
});

import { StyleSheet } from 'react-native';
import { Color } from '../../../utils/StylingConsts/Colors/Colors';

export const stylesSignUpOrLogin = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Color.colorWhite,
    padding: 30
  },
  heading: {
    fontFamily: 'Poppins Medium',
    fontSize: 16
  }
});

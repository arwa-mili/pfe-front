import { tt } from '../../../locales/translation.config';

export const MODAL_HEADERS_MESSAGES = {
  DELETION_ACTION: tt('Are you sure you want to delete that ?')
};
export const MODAL_BUTTONS_TEXTS = {
  ACCEPT: tt('OK'),
  YES: tt('YES'),
  REFUSE: tt('NO'),
  RETRY: tt('RETRY')
};

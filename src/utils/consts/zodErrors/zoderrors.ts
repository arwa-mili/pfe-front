import { tt } from '../../../locales/translation.config';
export const zoderrors = {
  isrequirederror: tt('field is required'),
  passwordShortError: tt('password must contain at least 8 characters')
};
